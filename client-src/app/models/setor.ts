export class Setor {
    setorId: number;
    codigo: number;
    descricao: string;
    constructor(setorId: number, codigo: number, descricao: string){
        this.setorId = setorId,
        this.codigo = codigo,
        this.descricao = descricao
    }
}
