import { EmpresaFilialDisabledOnSelectorDirective } from './empresa-filial-disabled-on-selector.directive';

describe('EmpresaFilialDisabledOnSelectorDirective', () => {
  xit('should create an instance', () => {
    //const directive = new EmpresaFilialDisabledOnSelectorDirective();
    //expect(directive).toBeTruthy();
  });
});
